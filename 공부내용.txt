0719

Shader Resource View

이게 터지면 파일 경로문제 이다.

srv = 파일 데이터 를 저장하는 변수

인스턴싱 

srv를 한개만 만들고 그걸 여러개 가져다 쓰면 된다.

srv에 이미지 데이터를 완전히 들어가게 된다.

RenderTexture

ArraySize 레이어가 여러개 있다.

그 레이어들이 몃개있나?

메탈릭, 그림자 표현 메테리얼 등

0724

ShetShader =  쉐이더 바꾸기

 Utilities 의 Aniamtor리소스를 보았을 떄, 픽셀의 계산위치로 보아서
한줄한줄 이미지를 하나씩 자른다고 보면된다.

uv좌표를 하나하나 위치를 연산해서 보여준다.

clipName으로 넣어두고 이름으로 계산한다.

Vector로 Key로 보관한다.

FrameCount 는 ㅐㅋ릭터의 개수, 1로 계산했을때 객체가 10개라면 10분의1 이다.

palyRate는 프레임 재생시간. 몃초마다 몃개씩 넘어가는지?

bReverse = 이미지의 좌우반전을  시도하는지?

죽는 에니메이션을 시도할떄, 안쪽에 bool변수 하나를 또 넣어서. 루프되는놈이니 아닌지 변수를 넣어서 처리해야 한다.

SetCurrentAnimClip 은, 자기자신이 다시 들어오는걸 막아야 하고,nullptr일떄, 애니메이션이 지정되어있지 않을 때 새로 넣어야 할때. 를 검사한다.


Texture2D 
텍스트 처리용으로.
tga일떄, dds 일경우 아닐경우. 다른방식으로 받아들여라. 텍스트를 읽어온다.

desc를 가져와서 확인한 다음 데이터를 긁어온다.

srv가 존재하는지 아닌지 확인해서 존재하지 않을경우. 새롭게 생성하라.
